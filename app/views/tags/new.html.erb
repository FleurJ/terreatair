<div class="container margin-top">
  <h1>Créer une nouvelle rubrique</h1>
  <hr>
  <%= simple_form_for @tag, url: tags_path do |c| %>
  <%= c.input :title, label: 'Titre' %>
  <%= c.input :description, label: 'Description' %>
  <%= c.submit "Créer", class: "btn btn-primary" %>
  <% end %>
</div>
